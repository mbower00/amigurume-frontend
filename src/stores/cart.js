import { ref, computed } from 'vue'
import { defineStore } from 'pinia'

export const useCart = defineStore('user', () => {
  // pull from local storage on refresh
  // add to local storage on change
  return {}
})
